export function TableCells(headings, rows){
    if ((Array.isArray(headings) && headings[0] && typeof headings[0] === "object" &&headings[0].headingName)&& (Array.isArray(rows) && rows[0] && typeof rows[0] === "object")){
        rows.forEach(row => {
            
        })

    }
}